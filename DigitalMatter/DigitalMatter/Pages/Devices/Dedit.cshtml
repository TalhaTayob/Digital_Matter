@page
@model DigitalMatter.Pages.Devices.DeditModel
@{
}

<br>

<h2>Edit a group</h2>

@if (Model.errorMessage.Length > 0)
{
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong>@Model.errorMessage</strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<form method="POST">
    <input type="hidden" name="id" value="@Model.dev.device_id">
    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Name of Device</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="devname" value="@Model.dev.device_name">
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label">Associated Group ID</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="GID" value="@Model.dev.group_id">
        </div>
    </div>

    @if (Model.success.Length > 0)
    {
        <div class="row mb-3">
            <div class="col-sm-9">
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <strong>@Model.success</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            </div>
        </div>
    }

    <div class="row mb-3">
        <div class="offset-sm-3 col-sm-3 d-grid">
            <button type="submit" class="btn btn-primary">Confirm</button>
        </div>
        <div class="col-sm-3 d-grid">
            <a class="btn btn-outline-primary" href="/" role="button">Back</a>
        </div>
    </div>

</form>

